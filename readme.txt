1. Create the Backup Script
   We'll write a shell script (backup.sh) that:
 
  - Creates a timestamped backup
  - Compresses the backup
  - Verifies the backup integrity
  - Let's create the script: backup.sh
  - To automate the backup execution, we’ll use a cron job.